# Use a custom conditional diffusion solver

Dreamaze's first version will use a small custom conditional diffusion model that takes a clean grid maze as input and generates a solution mask as output. We are not using Stable Diffusion for the first solver because the first goal is measurable maze solving, not general image generation or visual style.
